# =============================================================================
# Microcord Configuration
# =============================================================================
# Copy this file to .env and customise before running the container.
# All values below are defaults — uncomment and change what you need.
# These work for both `docker compose up` (dev) and `docker compose -f
# docker-compose.prod.yml up` (prod).
# =============================================================================

# ----- Authentication --------------------------------------------------------
AUTH_PROVIDER=local
# JWT_SECRET=                     # ≥32 chars; auto-generated when blank
# JWT_EXPIRY_HOURS=24
# REGISTRATION_PASSPHRASE=        # Upper-case phrase for registration; random when blank

# ----- Network ----------------------------------------------------------------
# CORS_ORIGIN=http://localhost:5173   # Dev default; set to origin URL in prod
# TRUST_PROXY=false                  # Set true behind a reverse proxy
# INSECURE_HTTP=false                # Set true for HTTP-only (no TLS)

# ----- UI (unprotected /api/config, used before login) -------------------------
# APP_NAME=🔊 Microcord
# APP_TAGLINE=Microcord — a mini self-hostable chat app
# VOICE_CHANNEL_NAME=Voice channel

# ----- Live media (protected /api/livemediaconfig, voice + screenshare) --------
# ICE_SERVERS=[{"urls":"stun:stun.l.google.com:19302"}]
# VOICE_ECHO_CANCELLATION=true
# VOICE_NOISE_SUPPRESSION=true
# VOICE_AUTO_GAIN_CONTROL=true
# VOICE_OPUS_BITRATE=32000             # 6 000–510 000 bps
# VOICE_OPUS_STEREO=false
# SCREENSHARE_WIDTH=1920
# SCREENSHARE_HEIGHT=1080
# SCREENSHARE_FRAME_RATE=60

# ----- Media processing (protected /api/livemediaconfig, ffmpeg) ---------------
# MEDIA_AVIF_CRF=30
# MEDIA_AV1_CRF=35
# MEDIA_VIDEO_SCALE=                   # e.g. 0.5 to halve dimensions
# MEDIA_VIDEO_MAX_BITRATE=             # e.g. 0.7M
# MEDIA_FFMPEG_THREADS=2

# ----- Uploads ----------------------------------------------------------------
# MAX_UPLOAD_SIZE_MB=50

# ----- TLS (prod only, mounted via /certs) ------------------------------------
# SSL_CERTFILE=/certs/fullchain.pem
# SSL_KEYFILE=/certs/privkey.pem
